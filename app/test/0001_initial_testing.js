const chai = require('chai');
const expect = chai.expect;
const assert = chai.assert;

describe('Template (0001)', function() {
    describe('Test Batch 1:', function() {
        it('No tests have been configured.');
    });
});